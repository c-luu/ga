// 6.1
// (TODO) Recurrence of contiguous subsequence of maxium sum:
function recContiguousSubsequence(): nat
{
    0
}

method contiguousSubsequence(a: seq<nat>) returns (b: seq<nat>)
ensures |b| < |a|
{
    assume |b| < |a|;
}